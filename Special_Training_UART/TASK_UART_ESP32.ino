void setup() {
  Serial.begin(115200);
  Serial1.begin(115200);
}
void loop() {
    // TASK: Insert code that will send a char to Arduino Uno using UART
    // (Use .print instead of .println to exclude new line)
    // Recommendation: Put a delay that is similar to that of Arduino Uno's code
}
